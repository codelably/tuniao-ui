/**
 * @file 毛玻璃效果分级枚举
 * @description 定义 backdropBlur 的 4 个级别（sm / default / md / lg）
 * @author JunBin.Yang
 */

export enum BLUR_LEVEL {
  /** 轻微模糊 */
  SM = 10,
  /** 默认模糊 */
  DEFAULT = 20,
  /** 中等模糊 */
  MD = 40,
  /** 强模糊 */
  LG = 60,
}
